info:
  name: Delete Specific Folder
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:8080/folder/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
