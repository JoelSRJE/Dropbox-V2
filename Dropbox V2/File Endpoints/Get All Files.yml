info:
  name: Get All Files
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/files
  headers:
    - name: Authorization
      value: Bearer {{AUTH_TOKEN}}
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
